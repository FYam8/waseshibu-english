# 英語本体 G0/G1 候補監査（2026-09-19）

対象は FYam8/waseshibu-english のみ。公開・mergeは未実施。
本書と tests は `_config.yml` により Pages の対象外。

## 固定点と公開方式

- 開始main / 調査基準: `ff379eb24808449f0387e58b0dcc493453dc9531`。
- 開始時の進行中PR: 0件。今回の作業ブランチ: `fix/english-g0-g1-isolated-audit`、draft PR #20。
- 旧公開: 同SHAの Pages run `35358131669` 成功。Jekyll build job `105642440986` と deploy job `105642616201` を確認。
- リポジトリに pages.yml は存在しない。GitHub生成の branch-based Jekyll publisher。公開indexはmainのroot indexとバイト一致。設定画面のsource情報は未取得。
- branchの既存verifyはテストのみ。AI benchmarkはmain限定の手動実行。新規workflowは contents:read のbuild/test/artifactのみで、Pages/Workers公開ステップを含まない。
- 初期候補 `0df77bb418cca973eed4b64196dd758989ad5f95`。その後テスト側のabout:blank投入を修正。最終候補SHAとCI runはPRに記録する。
- 本番index SHA-256: `47518b83349fffc2d504ad0f0473f0fff2313641bd1fd032d185df512e039bc7`。
- 本番旧検査HTML SHA-256: `30344446abe21b37023160f7ba1c1367f9dd06016fe14079b2a15e2c8bd1576e`。
- 上記2ファイルはHTTP GETのみで取得。危険な本番検査HTMLはブラウザで開いていない。
- 候補の読み込み順とハッシュ: `g0-g1-assets.json`（順序付き）。データ補正→学校/Engine→共通UI mount→app→compat→progress-sync。CSSは学校theme→共通base→学校compat。

## 保護対象と実フィールド

| 契約 | 現行値・対応 |
|---|---|
| 主保存 / legacy | `waseshibu.adaptive.v3` / `waseshibu.adaptive.v2` |
| schema | 8 |
| 移行 / import復旧 | `waseshibu.adaptive.pre-migration.v*` / `waseshibu.adaptive.pre-import.*`（3世代） |
| 同期DB | `waseshibu-progress-sync` version 7 |
| DB stores | control / outbox / deadletter / seen_v2 |
| 進捗appId / backup appId | english / waseshibu-english-adaptive |
| 進捗API | waseshibu-progress-api.fyam8.workers.dev |
| AI API | waseshibu-writing-grader.fyam8.workers.dev |
| 初回得点・初回答案 | attempts[].writtenScore / answers / manual、history、exposure |
| 過去問途中 / 英作文途中 | currentAttempt、answers、manual[].answerText |
| Today / 日数・日付境界 | dailyPlan、dailyProgress、drillLog、attempt timestamps |
| Resume / checkpoint相当 | currentDrill、currentSkill、recoveredDrills |
| 選択肢・並び・入力 | currentDrill.choiceOrder / selectedMany / order / orderIndices / textDraft / textInputs / selfText / selfParts / selfChecks |
| pending / mastery | weak[].status / streak / confirmStreak / next / reservedConfirm / seenDrills |

正規のimport置換、旧import復旧領域の3世代超削除、途中ドリルのユーザー削除はそのまま。
同期の登録・outbox整理も変更しない。移行は旧データの退避を伴い同じ主キーへ保存する。
初回候補fde3499ではapp.js / learning-model.js / engine / ui / 学校設定 / progress-sync / 教材の変更はない。追加の境界検査でapp.jsのupdateTimerに既存画面の再描画を追加した。詳細はg1-boundary-followup.md参照。

## 不具合台帳

| ID | 重大度・状態 | 再現・影響 | 候補対処・回帰 |
|---|---|---|---|
| G0-001 | P0、配信と実行可能な消去コードを確認。本人の履歴損失は未観測 | `tests/waseda-browser-smoke.html` がHTTP 200で全消去コードを含む。同一originの他アプリを含むlocalStorageに影響し得る。通常indexの動作による消去ではない | 旧URLはscriptなしの案内へ置換。fixtureを非HTMLソースに分離。Jekyllでtests等を除外。旧smokeはtemp siteと新規profileだけで実行。ガードに故障注入 |
| G0-002 | P1、検査構成差を確認 | 旧smokeはstyles.cssと旧header/navを使用し、共通UIと本番syncを通していない。既存CLEANを本番UI保証にできない | 旧characterizationを残し用途を明記。新規G1はJekyllが出力した未改変indexと全参照assetをハッシュ照合して使用 |
| G1-001 | P2、文字列3箇所を確認・候補修正 | indexのCSS間2箇所、header/nav間1箇所に文字のbackslash-n。HTML解釈上不要な本文文字となる | 実改行へ置換。静的回帰と本番入口のbody文字確認 |
| TEST-001 | 検査コードの不具合、修正済み | 初期CIはabout:blankにダミーキー投入を試みSecurityErrorを2件記録。学習サイクル自体は通ったが総合判定FAIL | seedをローカルoriginに限定。pageerrorは引き続き全件FAILとし、例外を握り潰していない |
| TEST-002 | 検査の比較対象の誤り、修正済み | selfcheckのreloadでtextarea.valueは保持されたが、初期HTMLを表すtextContentも比較していたためFAIL | 入力はlive valueと保存stateで比較し、textContent比較はbuttonラベルに限定。答案の比較は弱めていない |

候補修正は本番のリスク解消を意味しない。本番の危険ページが除外されたことは公開後に非実行HTTPで別確認が必要。

## 母集団・得点・学習条件

データ補正をindex順に適用したVM検査では、2019〜2023各22問、2024〜2026各19問、合計167問。各年度points合計80。
設問形式はchoice / pair / text / manual / multi。類題604件中active283 / retired321、類題ID重複0。
active形式はpair / text_multi / choice / selfcheck / multi_choice / text の6種類。`reorder`型のactive類題は0件。語順問題はpairとして存在するため、語順問題自体がないという意味ではない。
stable ID一覧と形式・弱点対応は `g0-g1-inventory.json`。実ブラウザ結果にもinventory.jsonを保存する。

学校設定は筆記80 / リスニング20 / 合計100。目標60/70/75は100点尺度の学習目標であり、学校の公式合格最低点や合格保証ではない。
原本PDF・公式配点の再照合は未実施。各年度のコード上の80点合計を公式確認と呼ばない。
年度順は2024→2023→2022→2021→2020→2019→2025→2026。
即時3連続→翌日2連続、標準目安10問を維持。時間を進める試験や人工得点は人の定着実証ではない。

## 検証定義・証拠

- ローカル実行: `node tests/g0-publication.test.mjs`、`node --check tests/g0-g1-browser.test.mjs`、`bash -n tests/waseda-browser-smoke.sh`、既存verifyのNode67コマンド（一覧 `g0-g1-node-checks.json`）。すべてPASS。
- G0静的ガードの故障注入5件: 文字の改行、旧URLにscript復活、testsの配信除外解除、fixture全消去復活、共有作業ツリーでのserver起動。それぞれ期待通りFAIL。
- CI: `.github/workflows/g0-g1-audit.yml`。Jekyllビルドでtests/docs/workersが存在しないこと、indexと全参照assetのバイト一致を検査。
- ブラウザ: `node tests/g0-g1-browser.test.mjs _site 390` / `1280`。Playwright 1.62.1 / Chromium、各テスト独立context、loopbackの別origin、Pagesサブパス再現、ServiceWorker禁止。
- 全外部HTTP通信をrouteでabort。同期設定・本番JSは書き換えない。匿名登録POSTが遮断されたことをblockedログで確認。実AI呼出しも遮断するためモデル精度や実サービス成功は検証しない。
- 代表過去問2024大問3問1: UIで初見/無制限を選択し誤答、記述draft保存、reload、採点、弱点、対応発音ドリル、同じ選択肢順でreload、正答、結果の二重保存防止、ホーム。
- 記述の満点自己採点は人工fixture入力。ほかの客観設問は未回答0点を確認ダイアログで明示承認する。29/80等は人工結果であって本人の点数ではない。
- バックアップ: UIからExport、独立contextへImport、同じファイル再Import、不正JSON。answers/manual/attempts/history/drillLog/weak/exposureの意味一致を比較。
- 許容する変化: 新規端末の同期control、復元前バックアップ、画面位置・dailyPlanの再生成。答案・過去の得点・弱点の意味は差を許容しない。
- active全6形式から各1代表の人工途中状態を投入して、実UIで選択/取消/文字入力・reload後の復元を比較。6/283の形式代表であり、全類題監査ではない。
- 2種類の他アプリdummyキーが変化しないことを確認。本人の履歴・認証情報は投入しない。
- screenshotとDOM本文、inventory、network、page errors、人工backupをCI artifactへ保存。console.errorは遮断URLと一致するERR_BLOCKED_BY_CLIENT（およびローカルfavicon 404）のみ予期済みとして扱う。
- 初期FAIL run: 35436631993（push）、35436660404（PR）。候補アプリの失敗と検査側TEST-001を混同しない。

## 範囲と残り

| ゲート | 今回の対象 | 未実施・除外 |
|---|---|---|
| G0 | 配信除外、全消去排除、隔離通信、人工履歴の移行/Import/再開 | 本人の全履歴実データの検証、本番除外確認は未実施 |
| G1 | 代表学習サイクル、6形式の途中保存、AI失敗時、得点分母、2画面幅 | 全年度・全問UI、全採点境界、日付境界の実UI、年度切替の全組合せ、AI timeout/不正応答、実機、全表示の精査は残る |
| G2 | 母集団・ID・形式・配点合計の機械確認 | 原本に照合した教材監査0/167、類題の意味内容の原本照合0/283。全問正答保証なし |
| G3 | 通信遮断時の答案保護と自己採点未確定 | 実モデル精度、独立した人手評価、gold/holdout評価は未実施 |
| G4/G5 | 人工データでフロー確認 | 本人の初見得点・自力再現・定着は未測定 |

2回連続CLEANはアプリ全体に対して宣言しない。固定した候補の実施範囲・各CI run・画面確認をPRに記録する。テスト定義変更時はカウントをリセットする。
次の優先作業はG1の未実施ケースを補い、公開候補の安全性と操作を確定すること。G2〜G5の広範囲改訂は先行させない。

## 適用と戻し方

本PRのmerge/Pages公開にはこの会話での明示許可が必要。現時点ではdraft候補のみ。
許可後もmainの競合確認・固定版の再検査・2回CLEAN・本番配信hashと危険URL不在を確認する。
戻す場合はコード/配信物だけを対象にし、localStorageやIndexedDB、クラウド履歴を巻き戻さない。危険な旧検査URLの復活は避ける。
